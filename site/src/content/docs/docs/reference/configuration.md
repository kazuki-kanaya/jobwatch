---
title: Configuration
description: jobwatch.yaml の基本設定。
---

`jobwatch init` で `jobwatch.yaml` を生成し、通知先やメタデータを設定します。

主な設定項目:

- `workspace`: 送信先ワークスペース
- `host`: 実行ホスト名
- `notifications`: 通知設定（例: Slack Webhook）
