---
title: CLI Reference
description: Obsern CLI の主要コマンド。
---

主要コマンド:

```bash
obsern --version (or: obsern version)
obsern init
obsern run <command>
obsern --help
```

基本フロー:

1. `obsern init` で `obsern.yaml` を生成
2. 必須の通知/認証設定を追加
3. `obsern run <command>` で実行
