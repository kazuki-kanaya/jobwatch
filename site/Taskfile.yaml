version: "3"

tasks:
  default:
    desc: Show available tasks
    cmds:
      - task --list

  build:
    desc: Build Astro site
    cmds:
      - pnpm build

  ci:
    desc: Run site CI checks
    cmds:
      - task: build

  dev:
    desc: Run Astro development server
    cmds:
      - pnpm dev
