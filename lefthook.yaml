pre-commit:
  parallel: true
  jobs:
    - name: api ci
      glob: "api/**/*.py"
      run: task api:ci

    - name: cli ci
      glob: "cli/**/*.go"
      run: task cli:ci

    - name: web ci
      glob: "web/**/*.{ts,tsx,js,jsx}"
      run: task web:ci
