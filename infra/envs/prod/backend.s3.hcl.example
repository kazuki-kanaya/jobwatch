bucket         = "obsern-terraform-state-prod"
region         = "ap-northeast-1"
profile        = "default"
dynamodb_table = "obsern-terraform-locks"
encrypt        = true

# Do not set `key` here.
# Each stack has its own key in backend.tf.
