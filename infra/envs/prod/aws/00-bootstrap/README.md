# Optional Bootstrap

Use this stage only if you want Terraform backend bootstrap resources
(such as S3 state bucket and DynamoDB lock table) managed separately.

Current repo uses per-directory local state by default.
